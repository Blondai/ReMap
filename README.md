# Remap

This lib aims to provide tools when handling [csv](https://en.wikipedia.org/wiki/Comma-separated_values) files.

The core functionality is reordering columns of a row of a csv file.

# TODOs

- [ ] Add `#[inline]` hints.
- [ ] Add methods with `fallback` argument.
- [ ] Add examples to docstrings.
- [ ] Add `CSV` struct (Wrapper around `Vec<Row>`).
- [ ] Refactor `Row` using generics.
- [ ] Add `MapBuilder` struct to increase usability (stores `HasMap`).
- [ ] Add support for `Map` instances from a file.
- [ ] Add file import/export.
